card editors {
  into ~/.config
  outof .config

  nvim/init.vim

  spark card vimPlugins
}

card vimPlugins {
  outof submodules
  into ~/.local/share/nvim/site/autoload

  vim-plug/plug.vim -> plug.vim
}

